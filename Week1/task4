class Solution4 {
    func fib(_ N: Int) -> Int {
        
        if(N == 0){
            return 0
        }else if (N == 1){
            return 1
        }
        var res = fib(N-1) + fib(N-2)
        return res
    }
}
